export default function MapColumn({ children }) {
    return <div className="h-full text-center relative inline-block bg-white" style={{ position: "relative", display: "inline-block", backgroundColor: 'white' }}>
        {children}
    </div>
}